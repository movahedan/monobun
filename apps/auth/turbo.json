{
	"extends": ["//"],
	"tasks": {
		"db:generate": {
			"inputs": ["src/prisma/**"],
			"outputs": ["../../node_modules/.prisma/auth-client/**"]
		},
		"typecheck": {
			"dependsOn": ["db:generate"]
		},
		"build": {
			"dependsOn": ["db:generate"]
		},
		"test": {
			"dependsOn": ["db:generate"]
		}
	}
}
